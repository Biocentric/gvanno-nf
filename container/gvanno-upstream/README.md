# Vendored upstream: `sigven/gvanno`

Pristine copy of `src/gvanno/` from [sigven/gvanno](https://github.com/sigven/gvanno),
pinned to the commit in `UPSTREAM_COMMIT`. **Do not edit these files** — changes
live in `../patches/` and are applied at image build time, so the delta stays
auditable.

Redistributed under the MIT licence in `LICENSE.md` (Copyright © 2026 Sigve
Nakken), added upstream in
[`b25acdf`](https://github.com/sigven/gvanno/commit/b25acdf4cedda081d11bbf89ac537615ae1b7c63).
The MIT terms require that notice to accompany copies, which is why
`LICENSE.md` sits here beside the code and is copied into the image.

The pinned commit **is** the licence commit. Its only difference from the
previously pinned `379ee24` is `LICENSE.md` and `CODE_OF_CONDUCT.md` —
`src/gvanno/` is byte-identical, so behaviour matches everything v0.3.0's gates
validated.
